{
  "analysis": {
    "title": "input_jp.json에는 있지만 api_jp.json에 없는 항목들",
    "date": "2025-07-11",
    "description": "input_jp.json의 addFullCompanySetup API와 api_jp.json의 개별 API들 간의 누락된 항목 분석"
  },
  "missing_items": {
    "companyInfo_missing_fields": {
      "companyName": {
        "type": "string",
        "description": "企業名（必須）",
        "required": true
      },
      "companyNameKana": {
        "type": "string",
        "description": "企業名（カナ）（必須）",
        "required": true
      },
      "contactPersonFirstName": {
        "type": "string",
        "description": "担当者名（必須）",
        "required": true
      },
      "contactPersonLastNameKana": {
        "type": "string",
        "description": "担当者姓（カナ）（必須）",
        "required": true
      },
      "contactPersonFirstNameKana": {
        "type": "string",
        "description": "担当者名（カナ）（必須）",
        "required": true
      },
      "city": {
        "type": "string",
        "description": "市区町村（必須）",
        "required": true
      },
      "buildingName": {
        "type": "string",
        "description": "建物名・部屋番号（任意）",
        "required": false
      },
      "fiscalYearStart": {
        "type": "string",
        "description": "会計年度開始月（必須・選択）",
        "required": true
      },
      "fiscalYearEnd": {
        "type": "string",
        "description": "会計年度終了月（必須・選択）",
        "required": true
      }
    },
    "ownerLoginInfo_completely_missing": {
      "loginEmail": {
        "type": "string",
        "description": "ログインID（メールアドレス）（必須）",
        "required": true
      },
      "password": {
        "type": "string",
        "description": "パスワード（必須）",
        "required": true
      }
    },
    "optionSelection_completely_missing": {
      "appIntegration": {
        "type": "string",
        "description": "アプリ連携（必須・ラジオ：あり／なし）",
        "required": true,
        "options": ["あり", "なし"]
      },
      "safetyConfirmation": {
        "type": "string",
        "description": "安否確認（必須・ラジオ：あり／なし）",
        "required": true,
        "options": ["あり", "なし"]
      },
      "occupationalHealthIntegration": {
        "type": "string",
        "description": "産業医連携（必須・ラジオ：あり／なし）",
        "required": true,
        "options": ["あり", "なし"]
      }
    },
    "occupationalDoctorInfo_completely_missing": {
      "occupationalDoctorCount": {
        "type": "number",
        "description": "産業医の人数（必須・選択：1名〜20名）",
        "required": true,
        "range": "1-20"
      },
      "occupationalDoctors": {
        "type": "array",
        "description": "産業医の詳細情報（動的配列）",
        "required": true,
        "items": {
          "name": {
            "type": "string",
            "description": "産業医名（必須）",
            "required": true
          },
          "nameKana": {
            "type": "string",
            "description": "産業医名（カナ）（必須）",
            "required": true
          },
          "phone": {
            "type": "string",
            "description": "電話番号（必須）",
            "required": true
          },
          "responsibleOffice": {
            "type": "string",
            "description": "担当事業所（必須・選択）",
            "required": true
          },
          "officeCode": {
            "type": "array",
            "description": "事業所コード（必須・複数選択）",
            "required": true,
            "items": {
              "type": "string"
            }
          }
        }
      }
    }
  },
  "summary": {
    "total_missing_sections": 3,
    "missing_sections": [
      "ownerLoginInfo",
      "optionSelection", 
      "occupationalDoctorInfo"
    ],
    "partially_missing_sections": [
      "companyInfo"
    ],
    "total_missing_fields": 16,
    "impact": "api_jp.json의 개별 API들로는 input_jp.json의 통합 회사 설정 기능을 완전히 구현할 수 없음"
  },
  "recommendations": {
    "option1": "api_jp.json에 누락된 항목들을 추가하여 통합 API 구조 지원",
    "option2": "input_jp.json을 api_jp.json의 개별 API 구조에 맞게 분리",
    "option3": "두 구조를 모두 지원하는 하이브리드 API 설계"
  }
}
