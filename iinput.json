// Company Registration Form - Complete Structure
{
  "operationName": "addCompanyRegistration",
  "variables": {
    "companyRegistrationInput": {
      "companyInfo": {
        "companyName": "string",                    // 기업명 (필수)
        "companyNameKana": "string",                // 기업명 (카나) (필수)
        "companyCode": "string",                    // 기업 코드 (필수)
        "contactPersonLastName": "string",          // 담당자명 (성) (필수)
        "contactPersonFirstName": "string",         // 담당자명 (이름) (필수)
        "contactPersonLastNameKana": "string",      // 담당자명 (세이) (필수)
        "contactPersonFirstNameKana": "string",     // 담당자명 (메이) (필수)
        "phoneNumber": "string",                    // 전화번호 (필수)
        "postalCode": "string",                     // 우편번호 (필수)
        "prefecture": "string",                     // 도도부현 (필수, 선택)
        "city": "string",                           // 시구정촌 (필수)
        "address": "string",                        // 번지 (필수)
        "buildingName": "string",                   // 건물・방 번호 (선택)
        "fiscalYearStart": "string",                // 회계연도 시작월 (필수, 선택)
        "fiscalYearEnd": "string"                   // 회계연도 종료월 (필수, 선택)
      },
      "ownerLoginInfo": {
        "loginEmail": "string",                     // 로그인 ID (이메일 주소) (필수)
        "password": "string"                        // 패스워드 (필수)
      },
      "optionSelection": {
        "appIntegration": "string",                 // 앱 연동 (필수, 라디오: 있음/없음)
        "safetyConfirmation": "string",             // 안부 확인 (필수, 라디오: 있음/없음)
        "occupationalHealthIntegration": "string"   // 산업의 연동 (필수, 라디오: 있음/없음)
      },
      "occupationalDoctorInfo": {
        "occupationalDoctorCount": "number",        // 산업의 인원수 (필수, 선택: 1명~20명)
        "occupationalDoctors": [                    // 산업의 상세 정보 (동적 배열)
          {
            "name": "string",                       // 산업의명 (필수)
            "nameKana": "string",                   // 산업의명 (카나) (필수)
            "phone": "string",                      // 전화번호 (필수)
            "responsibleOffice": "string",          // 담당 사업소 (필수, 선택)
            "officeCode": [                         // 사업소 코드 (필수, 멀티선택)
              "string"
            ]
          }
        ]
      },
      "doctorLoginInfo": {
        "doctorLoginEmail": "string",               // 로그인 ID (이메일 주소) (필수)
        "doctorPassword": "string"                  // 패스워드 (필수)
      },
      "employeeChatOptions": {
        "employeeChatDisplay": "string"             // 직원과의 채팅 (필수, 라디오: 표시/비표시)
      }
    }
  }
}

// Company Info Only - Simplified Structure
{
  "operationName": "addCompanyInfo",
  "variables": {
    "companyInfo": {
      "companyName": "string",                      // 기업명 (필수)
      "companyNameKana": "string",                  // 기업명 (카나) (필수)
      "companyCode": "string",                      // 기업 코드 (필수)
      "contactPersonLastName": "string",            // 담당자명 (성) (필수)
      "contactPersonFirstName": "string",           // 담당자명 (이름) (필수)
      "contactPersonLastNameKana": "string",        // 담당자명 (세이) (필수)
      "contactPersonFirstNameKana": "string",       // 담당자명 (메이) (필수)
      "phoneNumber": "string",                      // 전화번호 (필수)
      "postalCode": "string",                       // 우편번호 (필수)
      "prefecture": "string",                       // 도도부현 (필수, 선택)
      "city": "string",                             // 시구정촌 (필수)
      "address": "string",                          // 번지 (필수)
      "buildingName": "string",                     // 건물・방 번호 (선택)
      "fiscalYearStart": "string",                  // 회계연도 시작월 (필수, 선택)
      "fiscalYearEnd": "string"                     // 회계연도 종료월 (필수, 선택)
    }
  }
}

// Owner Login Info
{
  "operationName": "addOwnerLoginInfo",
  "variables": {
    "ownerLoginInfo": {
      "loginEmail": "string",                       // 로그인 ID (이메일 주소) (필수)
      "password": "string"                          // 패스워드 (필수)
    }
  }
}

// Option Selection
{
  "operationName": "addOptionSelection",
  "variables": {
    "optionSelection": {
      "appIntegration": "string",                   // 앱 연동 (필수, 라디오: 있음/없음)
      "safetyConfirmation": "string",               // 안부 확인 (필수, 라디오: 있음/없음)
      "occupationalHealthIntegration": "string"     // 산업의 연동 (필수, 라디오: 있음/없음)
    }
  }
}

// Occupational Doctor Info - Conditional Display
{
  "operationName": "addOccupationalDoctorInfo",
  "variables": {
    "occupationalDoctorInfo": {
      "occupationalDoctorCount": "number",          // 산업의 인원수 (필수, 선택: 1명~20명)
      "occupationalDoctors": [                      // 산업의 상세 정보 (동적 배열)
        {
          "name": "string",                         // 산업의명 (필수)
          "nameKana": "string",                     // 산업의명 (카나) (필수)
          "phone": "string",                        // 전화번호 (필수)
          "responsibleOffice": "string",            // 담당 사업소 (필수, 선택)
          "officeCode": [                           // 사업소 코드 (필수, 멀티선택)
            "string"
          ]
        }
      ]
    }
  }
}

// Doctor Login Info
{
  "operationName": "addDoctorLoginInfo",
  "variables": {
    "doctorLoginInfo": {
      "doctorLoginEmail": "string",                 // 로그인 ID (이메일 주소) (필수)
      "doctorPassword": "string"                    // 패스워드 (필수)
    }
  }
}

// Employee Chat Options
{
  "operationName": "addEmployeeChatOptions",
  "variables": {
    "employeeChatOptions": {
      "employeeChatDisplay": "string"               // 직원과의 채팅 (필수, 라디오: 표시/비표시)
    }
  }
}